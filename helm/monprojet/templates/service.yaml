apiVersion: v1
kind: Service
metadata:
  name: {{ include "monprojet.fullname" . }}
  labels:
    {{- include "monprojet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monprojet.selectorLabels" . | nindent 4 }}
