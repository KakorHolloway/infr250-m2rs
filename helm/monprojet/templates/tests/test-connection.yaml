apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "monprojet.fullname" . }}-test-connection"
  labels:
    {{- include "monprojet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "monprojet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
